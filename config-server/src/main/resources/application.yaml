server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/0Mateciotti/TpFinalDiplomatura.git
          default-label: master
          search-paths: config-repo
